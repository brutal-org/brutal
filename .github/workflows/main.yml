name: check

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        toolchain: [gnu, llvm]

    steps:
      - uses: actions/checkout@v2

      - name: Install dependencies
        run: .github/workflows/setup.sh

      - name: Cache Toolchain (${{matrix.toolchain}})
        uses: actions/cache@v2
        env:
          cache-name: cached-cross-compiler
        with:
          path: |
            **/toolchain/
          key: |
            ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/meta/toolchain/${{matrix.toolchain}}/build.sh') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/meta/toolchain/${{matrix.toolchain}}/build.sh') }}

      - name: Build Toolchain
        run: |
          meta/toolchain/${{matrix.toolchain}}/build.sh

      - name: Build Kernel
        run: |
          make CONFIG_TOOLCHAIN=${{matrix.toolchain}} all

      - name: Run Tests
        run: |
          make CONFIG_TOOLCHAIN=${{matrix.toolchain}} run-test

